{
  "name": "@d1dx/paperclip-human-responder",
  "version": "0.1.0-pre",
  "description": "HTTP service that receives channel events (Google Chat, Telegram planned, Slack planned, ...) and posts back to Paperclip REST as the assigned agent. Companion to @d1dx/paperclip-human-adapter.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/server.js",
  "bin": {
    "paperclip-human-responder": "./dist/cli.js"
  },
  "files": [
    "dist",
    "Dockerfile",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "start": "node dist/server.js",
    "dev": "tsx watch src/server.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@d1dx/paperclip-human-bridge-shared": "workspace:*",
    "hono": "^4.6.13",
    "@hono/node-server": "^1.13.7",
    "better-sqlite3": "^11.7.0",
    "zod": "^3.24.1",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "@types/better-sqlite3": "^7.6.12",
    "vitest": "^2.1.8"
  },
  "keywords": [
    "paperclip",
    "paperclipai",
    "responder",
    "webhook",
    "google-chat",
    "telegram",
    "slack"
  ],
  "publishConfig": {
    "access": "public"
  }
}
