/**
 * @d1dx/paperclip-gchat-bridge-shared
 *
 * Shared library used by both the Paperclip plugin (`@d1dx/paperclip-gchat-adapter`)
 * and the responder service (`@d1dx/paperclip-gchat-responder`).
 *
 * Re-exports config schema, types, Chat + Paperclip clients, and the command parser.
 */

export * from "./config.js";
export * from "./types.js";
